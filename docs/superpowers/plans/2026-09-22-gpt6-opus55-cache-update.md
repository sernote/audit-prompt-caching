# GPT-6 Sol and Claude Opus 5.5 Cache Update Implementation Plan

> **For agentic workers:** Execute the steps below in order. The user requested an independent Porch review, which remains read-only; implementation stays in this branch.

**Goal:** Bring the portable prompt-cache skill and its usage accounting in line with the official GPT-6 Sol and Claude Opus 5.5 documentation available on 2026-09-22.

**Architecture:** Keep provider-specific API behavior in provider references, add a short agent-loop decision point to the main skill, and correct the stdlib usage analyzer where OpenAI cache writes are currently mistaken for Anthropic-style additive input. Extend the ROI estimator for measured write premiums and the repository scanner for new controls. Preserve backward compatibility for existing logs and CLI output. Treat the Telegram draft as a separate editorial artifact outside the skill package.

**Tech Stack:** Markdown, Python stdlib, unittest.

---

### Task 1: Fix usage accounting for GPT-5.6+ OpenAI writes

**Files:** `tests/test_prompt_cache_scripts.py`, `audit-prompt-caching/scripts/analyze_usage_logs.py`.

- [x] Add a test with `usage.input_tokens=1000`, `input_tokens_details.cached_tokens=400`, and `cache_write_tokens=300`. Assert total input remains 1000, cache hit ratio is 0.4, and write tokens are reported separately in summary and normalized events.
- [x] Run the focused unittest and observe failure for the new assertion; repeat RED for the write/read ratio.
- [x] Split `cache_write_tokens` from `cache_creation_input_tokens` in field aliases. Keep Anthropic/Bedrock creation and read fields additive; count OpenAI write tokens within `input_tokens`.
- [x] Run the focused test and full unittest suite.

### Task 2: Correct provider and agent guidance

**Files:** `audit-prompt-caching/references/{openai,anthropic,bedrock,economics,agent-tools}.md`, `audit-prompt-caching/SKILL.md`, `audit-prompt-caching/scripts/{estimate_cache_roi,extract_llm_calls}.py`, `tests/test_prompt_cache_scripts.py`.

- [x] Check the existing reference assertions for assumptions that the new model guidance makes false; preserve coverage for legacy models.
- [x] Update provider references with dated official links and model-specific distinctions. Keep legacy behavior clearly separated; avoid implying controls are universal across providers, models, or API surfaces.
- [x] Add routing guidance to the skill entrypoint and agent-tool reference for append-only effort/tool changes on supported models.
- [x] Add RED/GREEN tests and implementation for measured cache-write ROI and new OpenAI/Bedrock scanner signals.
- [x] Run focused tests and package/trigger validation.

### Task 3: Publication and verification

**Files:** This plan and changed skill files; Telegram draft in the final handoff.

- [x] Reconcile Porch's read-only findings against primary sources and the diff.
- [x] Run the repository verification commands and `git diff --check`; confirm no generated bytecode exists in this isolated worktree.
- [x] Review the changed-file list and diff against current provider guides. HTTP HEAD checks are region/access limited, so verify content through the official documentation pages rather than treating those HTTP results as broken links.
- [ ] Commit the scoped files, push `codex/cache-models-2026-09`, create an MR/PR, and attach it to this task.

**Primary sources:**

- OpenAI launch: https://openai.com/index/introducing-gpt-6-sol-and-luna/
- OpenAI cache guide: https://developers.openai.com/api/docs/guides/prompt-caching
- OpenAI diagnostics: https://developers.openai.com/api/docs/guides/prompt-caching/diagnostics
- Anthropic model page: https://platform.claude.com/docs/en/models/opus-5-5/overview
- Anthropic release changes: https://platform.claude.com/docs/en/models/opus-5-5/whats-new-opus-5-5
- Anthropic cache guide: https://platform.claude.com/docs/en/build-with-claude/prompt-caching
