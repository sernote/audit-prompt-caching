# GPT-6 Sol and Claude Opus 5.5 Cache Update

**Goal:** Refresh the portable skill for the provider contracts available on 2026-09-22 and open a reviewable PR.

**Context:** The branch began before several upstream provider and usage-adapter changes. After merging current `main`, retain its normalized usage accounting and ROI estimator. Add only the new model guidance and script support that is still missing. Keep the Telegram draft outside the skill package.

**Sources:** Live OpenAI and Anthropic cache, reasoning, effort, diagnostics, model and launch pages; AWS model cards; the Anthropic Opus 5.5 system card. Use API guides for cache contracts, not safety cards or older cookbooks.

## Tasks

- [x] Compare GPT-6 Sol, Opus 5.5, and AWS availability with official provider pages. Confirm that Anthropic currently lists Opus 5.5, not Opus 5.6.
- [x] Update OpenAI guidance: optional `prompt_cache_key` on GPT-5.6+, implicit lookup of earlier message boundaries, GPT-6 Sol pricing and controls, diagnostics and prewarm.
- [x] Update Anthropic guidance: Opus 5.5 token minimum, read/write prices, per-message effort, append-only tool changes, thinking, and diagnostics.
- [x] Update Bedrock availability and the cache write/read break-even explanation; keep provider usage accounting separate.
- [x] Add failing script tests for GPT-6 Sol and Opus 5.5 effort controls and explicit-breakpoint discovery; observe RED, implement minimal changes, observe GREEN.
- [x] Run all repository tests (238 passed), skill validation, trigger eval (40 cases), syntax, and whitespace checks after the upstream merge; confirm no generated bytecode.
- [x] Push the resolved branch and confirm the PR is mergeable or report the exact remaining block.

## Post-merge review follow-up for PR #27 (2026-09-23)

PR #27 merged at `22e1664` before the later Opus binding correction (`5e1ef41`)
and these review fixes, so carry both into a new PR from `origin/main`.

The AP-15 model-list correction increases the deferred reference estimate from
77,677 to 77,687 tokens (`ceil(chars/4)`), so the measured ceiling is updated
without deleting guidance.

- [x] Reproduce the reported ROI case with a nonzero dynamic suffix; retain the upstream static-only write model and add regression coverage.
- [x] Align AP-15's canonical rule and agent playbook with the linter's documented GPT-6 and Claude per-message effort model lists.
- [ ] Open a follow-up PR from `origin/main` and reply in both inline review threads with the evidence.
