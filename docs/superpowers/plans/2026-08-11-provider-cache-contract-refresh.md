# Provider Cache Contract Refresh Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Correctly audit current provider prompt-cache telemetry and request semantics across managed and self-hosted inference surfaces.

**Architecture:** Keep `analyze_usage_logs.py` as a small provider-shape normalizer with explicit inclusive/additive semantics. Keep volatile provider behavior in references, while trigger/eval coverage ensures the skill routes users to the correct provider-specific diagnostic path.

**Tech Stack:** Python 3 standard library, unittest, Markdown, JSON.

---

## File and Change Map

- `audit-prompt-caching/scripts/analyze_usage_logs.py`: Bedrock lower-camel and Gemini Interactions usage extraction.
- `audit-prompt-caching/scripts/extract_llm_calls.py`: detect current cache/routing controls.
- `tests/test_prompt_cache_scripts.py`: TDD regression coverage.
- `audit-prompt-caching/references/*.md`: provider-specific semantics and safe audit actions.
- `audit-prompt-caching/{SKILL.md,evals/evals.json,evals/trigger_eval.json,references/rules.json}`: trigger and audit guidance.

### Task 1: Normalize current Bedrock and Gemini usage

**Files:**
- Modify: `audit-prompt-caching/scripts/analyze_usage_logs.py`
- Modify: `tests/test_prompt_cache_scripts.py`

- [ ] Add a failing test for a Bedrock Converse payload using `inputTokens`, `cacheReadInputTokens`, `cacheWriteInputTokens`, and `outputTokens`; assert additive total input.
- [ ] Run `python3 -m unittest tests/test_prompt_cache_scripts.py` and confirm the new assertion fails before implementation.
- [ ] Extend Bedrock shape detection and extraction to accept both service-metric PascalCase and API lower-camel fields.
- [ ] Re-run the focused test and then the full unittest suite; confirm both pass.
- [ ] Add a failing Gemini Interactions test with `usage.total_cached_tokens`, `prompt_token_count`, and `candidates_token_count`; assert inclusive accounting and no ambiguous warning.
- [ ] Extend Gemini detection/extraction without changing unknown-wrapper semantics.
- [ ] Re-run focused and full tests; commit the telemetry change.

### Task 2: Refresh managed-provider references and agent guidance

**Files:**
- Modify: `audit-prompt-caching/references/{azure-openai,bedrock,gemini,anthropic,openrouter,qwen,deepseek,zai,agent-tools,observability}.md`
- Modify: `audit-prompt-caching/SKILL.md`
- Modify: `audit-prompt-caching/evals/{evals,trigger_eval}.json`
- Modify: `audit-prompt-caching/references/rules.json`

- [ ] Add tests/evals that request Azure GPT-5.6, OpenRouter `session_id`, Claude cache diagnostics, Gemini Interactions, Qwen Responses session cache, DeepSeek prefix-unit behavior, and preserved reasoning.
- [ ] Verify the added trigger/eval expectations fail or lack the required coverage before documentation/routing updates.
- [ ] Document only official current contracts: surface boundaries, usage fields, cache/write costs where necessary, and a safe validation action.
- [ ] Add agent checks for opaque session/cache-key hashes, diagnostics correlation, and preserved reasoning blocks; never log raw identifiers.
- [ ] Run trigger evaluation and package validation; commit the managed-provider change.

### Task 3: Refresh self-hosted KV cache guidance

**Files:**
- Modify: `audit-prompt-caching/references/{vllm,sglang}.md`
- Modify: `audit-prompt-caching/scripts/extract_llm_calls.py`
- Modify: `tests/test_prompt_cache_scripts.py`

- [ ] Add failing detector tests for KV-cache events, vLLM multi-tier/offload signals, and SGLang hierarchical-cache/disaggregated-serving signals.
- [ ] Run the focused tests and confirm missing detection before implementation.
- [ ] Add narrow patterns that detect signals without treating the presence of a flag as proof of cache effectiveness.
- [ ] Update vLLM/SGLang references with version-conditioned KV events, tier lifecycle, prefill/decode separation, and cache-aware routing observations.
- [ ] Run full unittest, package validation, and trigger evaluation; commit the self-hosted change.

### Task 4: Integrate and verify

**Files:** all changed files above.

- [ ] Cherry-pick the independently reviewed task commits.
- [ ] Resolve overlaps by retaining provider-specific terminology and the explicit inclusive/additive accounting boundary.
- [ ] Run `python3 -m unittest tests/test_prompt_cache_scripts.py`.
- [ ] Run `python3 audit-prompt-caching/scripts/validate_skill_package.py audit-prompt-caching`.
- [ ] Run `python3 audit-prompt-caching/scripts/run_trigger_eval.py audit-prompt-caching`.
- [ ] Compile all Python scripts and tests, run `git diff --check`, and remove generated bytecode.
- [ ] Request an independent Claude code review of the final diff; address verified findings, rerun verification, and commit.
