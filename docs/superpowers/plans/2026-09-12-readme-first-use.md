# README First-Use Refresh Implementation Plan

> Execute inline with `superpowers:executing-plans`; this is a documentation
> change. Script behavior and provider references stay outside this change.

**Goal:** Make the README a clear entry point for a first project audit while
preserving the existing runnable examples, coverage and evidence boundaries.

**Architecture:** Keep the README in English and lead with the problem, install
command and a copyable audit request. Use a compact before/after illustration
from the existing prefix fixture. Move the long script commands, synthetic
arithmetic and scenario prompts to one linked usage guide. Reuse the existing
first-audit, router-observation and contributing guides.

**Tech Stack:** GitHub-flavored Markdown; existing Python stdlib helpers.

**Base:** `origin/main` at `38ec461cc7a6e4c9a12c2d5d5535b9932016fc71`.
The main checkout contains unrelated changes, so work in a separate worktree.

## Implementation

- [x] Read the current README, skill entry point, existing guides and installer.
  Preserve project-first auditing, precise evidence limits, provider coverage,
  script semantics, example prompts, public project/channel links and MIT/CI.
- [x] Rewrite `README.md`: purpose → Quick Start → before/after example →
  task-based coverage → further examples and tools → contribution/field notes.
  Preserve `#quick-start` and useful existing section anchors where practical.
  Limit explanatory visuals to the prefix comparison and a compact audit result.
- [x] Add `docs/usage.md` for executable script examples, synthetic usage/ROI,
  advanced routing requirements and the six existing diagnostic prompts.
  Keep command prerequisites and expected nonzero diff exits explicit.
- [x] Update the existing documentation checks to follow material moved to
  `docs/usage.md`, while retaining README links to the first audit and examples.
  Preserve the locator and accounting contracts; rerun the full suite after
  documentation changes.
- [x] Verify the displayed fixture numbers and run the documented install into
  an isolated temporary directory. Check internal links, heading anchors,
  rendered desktop/mobile layout, package validation and repository checks.
- [x] Review the final diff for unsupported claims and lost useful content;
  commit the reviewed change and provide a concrete review artifact.

**Acceptance:** A reader can install and start an audit of their own repository
before encountering optional fixture or routing work. The page explains a real
result shape without promising cache hits, savings or native router-log support.
Existing detailed guidance remains reachable, and the README renders clearly
as a GitHub document without a custom website or new runtime dependencies.

## Verification record

- README reduced from 463 to 223 lines. One compact diff illustrates the
  existing prefix example; its adjacent table retains the measured values.
- Baseline: all 235 tests passed in 32.929 seconds before documentation edits.
  The first PR check then found five documentation assertions still targeting
  the old README. All five failures were reproduced locally before correction.
- Final local run: all 235 tests passed in 33.482 seconds after adapting those
  assertions to the linked usage guide. Follow-up review confirmed that the
  locator, accounting, linter-example and scenario contracts remain covered.
- Eleven command checks passed: prefix comparisons (expected exit 1, 43/254
  bytes), usage and normalized usage, ROI, two clean layouts, report, routing,
  package validation and the 40-record trigger dataset check.
- The ROI command now reproduces the displayed $34.60 → $23.10 scenario.
- Both the Bash installer and public `npx skills add` installed into isolated
  temporary directories. Each installed package matched all 43 source files.
- Verified 56 local links/fragments, preserved the existing Quick Start anchor,
  checked Python syntax/whitespace and confirmed no generated bytecode.
- GitHub-rendered Markdown was inspected at desktop 1280px and mobile 390px.
  Neither document overflowed the page, including expanded details. Shortened
  the explanatory diff and starter prompt after inspecting mobile screenshots.
- Independent review found no blocking issue or material content loss. Fixed
  its minor clarification: usage accounting can derive warning/fail even when
  its status was omitted. Preview files remain outside the repository.
