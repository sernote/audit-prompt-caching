# README Front Door

## Goal

Make the repository easier to understand and try from the first screen without renaming the package or adding distribution/community infrastructure.

## Scope

- Rewrite the README opening around the problem, visible audit output, quick start, fixture numbers, and positioning.
- Add badges for CI, license, Python, stdlib-only scripts, and Codex skill compatibility.
- Add a cache-flow diagram that makes the audit target visible.
- Strengthen the `SKILL.md` trigger description without broadening it into generic LLM cost advice.
- Add tests that keep these README and trigger-surface assets from disappearing.

## Out Of Scope

- No repository rename or skill folder rename.
- No `install.sh`, package manager release, or hosted CLI.
- No issue templates, release tags, changelog release process, or community hooks.
- No unverifiable competitor benchmark claims.

## Verification

- `python3 -m unittest tests/test_prompt_cache_scripts.py`
- `python3 audit-prompt-caching/scripts/validate_skill_package.py audit-prompt-caching`
- `python3 audit-prompt-caching/scripts/run_trigger_eval.py audit-prompt-caching`
- Python syntax compile check from `AGENTS.md`
- `git diff --check`
- Generated bytecode guard
