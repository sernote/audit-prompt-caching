interface:
  display_name: "Prefix Cache Audit"
  short_description: "Audit LLM prompt-cache efficiency"
  default_prompt: "Use $audit-prompt-caching to audit this LLM app for prompt-cache misses, TTFT regressions, and ROI."
